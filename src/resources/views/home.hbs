<div class="mt-4 text-center align-items-center">
    <div class="row">
        {{#each gundams}}
        <div class="col-sm-6 col-lg-3">            
            <div class="card card-items">
                <img class="card-img-top" src="{{this.imgage}}" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">
                        {{this.name}}
                    </h5>
                    <p class="card-text">
                        {{this.price}}
                    </p>
                    <a href="/products/{{this.slug}}" class="btn btn-primary">
                        Xem chi tiết
                    </a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>